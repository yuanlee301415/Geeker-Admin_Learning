/**
 * ElementPlus 组件大小
 */
export enum AssemblySize {
    Default = 'default',
    Large = 'large',
    Small = 'small'
}

/**
 * ElementPlus 语言
 */
export enum Language {
    Zh = 'zh',
    En = 'en'
}

/**
 * 布局样式
 */
export enum Layout {
    Vertical = "vertical",
    Classic = "classic",
    Transverse = "transverse",
    Columns = "columns"
}
