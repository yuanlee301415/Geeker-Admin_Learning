/*
 * 枚举常量
 * */

// ElementPlus 组件大小
export enum AssemblySize {
  Default = 'default',
  Large = 'large',
  Small = 'small'
}

// ElementPlus 语言
export enum Language {
  Zh = 'zh',
  En = 'en'
}

// 布局方式
export enum Layout {
  // 纵向
  Vertical = 'vertical',
  // 经典
  Classic = 'classic',
  // 横向
  Transverse = 'transverse',
  // 分栏
  Columns = 'columns'
}

// 主题
export enum ThemeTypeEnum {
  // 浅色
  Light = 'light',
  // 深色
  Dark = 'dark',
  // 反转
  Inverted = 'inverted'
}

// 灰色或色弱
export enum GreyOrWeakEnum {
  Grey = 'grey',
  Weak = 'weak'
}
